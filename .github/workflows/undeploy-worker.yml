name: Undeploy worker

on:
  workflow_dispatch:

jobs:
  undeploy:
    runs-on: ubuntu-latest
    steps:
      - name: Delete Places enrichment worker
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        run: npx wrangler delete places-enrich --force
